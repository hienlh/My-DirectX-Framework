#include "ScrollingWheelController.h"
#include "PlayerController.h"

void ScrollingWheelController::OnCollisionEnter(Framework::CCollision* collision)
{

}

void ScrollingWheelController::Update(const DWORD& dt)
{
	if (m_player->GetComponent<PlayerController>());
}
