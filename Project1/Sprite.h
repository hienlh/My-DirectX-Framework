#pragma once
#include "Header.h"

class CSprite
{
/*private:
	unsigned short m_index;
	RECT m_rect;
	LPDIRECT3DTEXTURE9 m_texture;

public:
	CSprite()
	{
		m_index = 0;
		m_rect = { 0 };
		m_texture = nullptr;
	}
	~CSprite()
	{
		m_texture->Release();
	}

public:
	void Set_Index(unsigned short index) { m_index = index; }
	void Set_Rect(RECT rect) { m_rect = rect; }
	void Set_Texture(LPDIRECT3DTEXTURE9 texture) { m_texture = texture; }*/
};